# Step 8 — High fidelity wireframe

## Goal
Recreate the same screens as the low-fidelity wireframes (step 6), adopting
the branding guide (step 7): colours, typeface, logo, and UI components.

## Inputs
- `wireframes/` — low-fidelity wireframes (step 6 deliverable)
- `branding/` — branding guide (step 7 deliverable)

## Acceptance criteria
Same screens as the low-fidelity wireframes, but adopting the branding guide.
May be combined with the Prototype (step 9) if that makes sense — if you
choose to combine them, call the ask_human tool (type "improvement") to
confirm before doing so.
